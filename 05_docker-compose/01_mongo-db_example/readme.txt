
# write an docker-compose yml file 
# ex:  docker-compose.yml 
#      mongo-express.yml 

# Build
# $ cd <path to project>
# docker-compose build

# Output
# mongo uses an image, skipping
# mongo-express uses an image, skipping

#####################

# To start:
# $ docker-compose -f mongo-express.yml up -d

#    note: -d to run it in detached mode

# to stop:
# $ docker-compose -f mongo-express.yml down


